/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.logo.dev"], // Add the domain(s) you're using
  },
  reactStrictMode: true,
};

export default nextConfig;
